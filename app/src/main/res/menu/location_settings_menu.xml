<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/search_location"
        app:showAsAction="always|collapseActionView"
        android:title="search"
        android:icon="@mipmap/ic_launcher_search"
        app:actionViewClass="android.support.v7.widget.SearchView"
        />
    <item android:id="@+id/add_location"
        app:showAsAction="always|collapseActionView"
        android:title="Add Location"
        android:icon="@drawable/ic_add_location_"/>

    <item android:id="@+id/beacon_settings"
        app:showAsAction="never"
        android:title="Beacon Settings"/>

</menu>